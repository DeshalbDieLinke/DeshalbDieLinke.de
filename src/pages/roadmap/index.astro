---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.tsx';
import Footer from '../../components/Footer.tsx';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import QuestionAndAnswer from '../../components/QuestionAndAnswer';
const pathname = Astro.url.pathname.replace(import.meta.env.BASE_URL, '');
const subpath = pathname.match(/[^\/]+/g);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
        <Header client:load pathname={pathname} subpath={subpath} />
		<main class="w-full h-full p-10 font-bold text-2xl">
            <p>Diese Seite ist noch im Aufbau. 
            <section id="steuern">
                <h2 class="">Steuern</h2>
                <p class="text-sm font-light"> Die Steuern Chart funktioniert in ECharts nicht so, wie wir es brauchen; Die Label sind nicht bewegbar und die TimeLine checkpoints auch set. Dadurch entstehen unschöhne overlaps usw. 
                    Lösung: Wir werden das Feature vom boden auf neu bauen. Daher ist es momentan in absolutem Minimalzustand.
                </p>
            </section>
		</main>
		<Footer client:load/>
	</body>
</html>

<style>
	h5 {
		font-family: "work sans";
	}
</style>

